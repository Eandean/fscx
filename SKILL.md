[# 📋 学业水平评价成绩查询系统（fscx）

> 一个基于 **Cloudflare Workers + D1 + KV** 构建的轻量级学业成绩查询平台。  
> 支持学生自助查询、教师工作台、管理后台三套界面，单文件部署，零运维成本。

---

## 📑 目录

- [项目简介](#项目简介)
- [功能一览](#功能一览)
- [系统架构](#系统架构)
- [部署方式一：命令行（Wrangler CLI）](#部署方式一命令行wrangler-cli)
- [部署方式二：Cloudflare 官网手动部署（小白推荐）](#部署方式二cloudflare-官网手动部署小白推荐)
- [本地开发与运行](#本地开发与运行)
- [初始化数据库](#初始化数据库)
- [功能详解](#功能详解)
- [安全机制说明](#安全机制说明)
- [常见问题](#常见问题)

---

## 项目简介

本项目是一套**为学校考试成绩查询场景**量身设计的 Web 应用，学生只需凭准考证号等信息即可自助查询成绩，无需安装任何客户端。

**核心特点：**

- 🚀 **单文件部署**：整个后端仅一个 `fscx-d1.js` 文件，部署极为简单
- 💸 **免费运行**：Cloudflare Workers 免费版每天 10 万次请求，D1 数据库免费额度足够中小学使用
- 🔒 **安全可靠**：内置暴力破解防护、Token 认证、XSS 防护
- 📱 **手机自适应**：前台、教师端、管理后台均完整适配移动端
- 🎨 **高度可定制**：页面标题、查询字段、科目方向等均可在后台灵活配置，无需改代码
- 📊 **多角色支持**：学生查询 / 教师工作台 / 管理员后台三套独立界面

---

## 功能一览

| 模块 | 功能 |
|------|------|
| **学生前台** | 成绩查询、历次成绩对比、成绩等次显示 |
| **教师工作台** | 按班级查看成绩、年级排名、各科统计 |
| **管理后台** | 录入/编辑/删除成绩、批量导入、统计概览、系统配置 |
| **安全防护** | 登录频率限制、Token 会话、XSS 转义 |

---

## 系统架构

```
用户浏览器
    │
    ▼
Cloudflare Workers（fscx-d1.js）
    ├── 路由分发
    ├── 业务逻辑
    ├── 读写 D1（SQLite）→ 存储成绩 & 配置
    └── 读写 KV（键值）→ 存储教师账号 & 频率限制 & Token
```

**数据库表结构：**

- `config` 表：存储页面配置（标题、字段、科目方向等），键值对形式
- `scores` 表：存储学生成绩，以 `exam_id + uid` 为联合主键

---

## 部署方式一：命令行（Wrangler CLI）

> 适合有一定命令行基础的用户，操作最高效。

### 第一步：准备工作

确保你的电脑已安装 **Node.js**（版本 18 及以上）。

打开命令提示符（Windows 按 `Win+R` 输入 `cmd`，Mac 打开"终端"），输入以下命令检查：

```bash
node -v
```

如果显示版本号（如 `v20.x.x`）则说明已安装。若未安装，请前往 [https://nodejs.org](https://nodejs.org) 下载安装。

### 第二步：安装 Wrangler

```bash
npm install -g wrangler
```

安装完成后验证：

```bash
wrangler --version
```

### 第三步：登录 Cloudflare

```bash
wrangler login
```

执行后会自动在浏览器打开 Cloudflare 授权页，点击「Allow」即可。

### 第四步：下载项目文件

将以下文件放在同一个文件夹里：

```
my-fscx/
├── fscx-d1.js      ← 主程序
├── schema.sql      ← 数据库初始化脚本
└── wrangler.toml   ← 部署配置文件
```

在命令行中进入该目录：

```bash
cd 你的文件夹路径
# 例如 Windows：
cd C:\Users\你的用户名\Desktop\my-fscx
# 例如 Mac/Linux：
cd ~/Desktop/my-fscx
```

### 第五步：创建 D1 数据库

```bash
wrangler d1 create fscx-db
```

执行成功后，终端会输出类似以下内容：

```
✅ Successfully created DB 'fscx-db'
{
  "database_id": "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
}
```

**重要：** 把这个 `database_id` 复制下来，填入 `wrangler.toml` 文件中：

```toml
[[d1_databases]]
binding       = "DB"
database_name = "fscx-db"
database_id   = "在这里粘贴你的 database_id"
```

### 第六步：创建 KV 命名空间

```bash
wrangler kv namespace create KV
```

同样，将输出的 `id` 填入 `wrangler.toml`：

```toml
[[kv_namespaces]]
binding = "KV"
id      = "在这里粘贴你的 KV id"
```

### 第七步：初始化数据库表结构

```bash
wrangler d1 execute fscx-db --file=schema.sql
```

### 第八步：设置管理员密码

```bash
wrangler secret put ADMIN_PASSWORD
```

执行后会提示你输入密码，输入完成按回车（输入时终端不会显示任何字符，这是正常的）。

### 第九步：部署

```bash
wrangler deploy
```

部署成功后终端会显示你的访问地址，格式为：

```
https://fscx.你的用户名.workers.dev
```

🎉 **至此部署完成！** 访问该地址即可使用系统。

---

## 部署方式二：Cloudflare 官网手动部署（小白推荐）

> 全程鼠标操作，不需要安装任何软件，适合不熟悉命令行的用户。

### 第一步：注册 Cloudflare 账号

前往 [https://dash.cloudflare.com/sign-up](https://dash.cloudflare.com/sign-up) 注册账号（免费）。

### 第二步：创建 D1 数据库

1. 登录后，点击左侧菜单 **「Workers & Pages」**
2. 找到并点击 **「D1 SQL 数据库」**（或在左侧找到 **「D1」**）
3. 点击右上角 **「创建数据库」**
4. 数据库名称填写：`fscx-db`
5. 点击 **「创建」**

创建完成后，你会看到这个数据库的页面。

**初始化表结构：**

1. 在数据库页面，点击 **「控制台」** 标签页
2. 将下面的 SQL 复制粘贴进去，点击 **「执行」**：

```sql
CREATE TABLE IF NOT EXISTS config (
  key   TEXT PRIMARY KEY,
  value TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS scores (
  exam_id   TEXT    NOT NULL,
  uid       TEXT    NOT NULL,
  fields    TEXT    NOT NULL DEFAULT '{}',
  direction TEXT    NOT NULL DEFAULT '',
  scores    TEXT    NOT NULL DEFAULT '{}',
  PRIMARY KEY (exam_id, uid)
);

CREATE INDEX IF NOT EXISTS idx_scores_uid ON scores(uid);
CREATE INDEX IF NOT EXISTS idx_scores_exam ON scores(exam_id);
```

**记录数据库 ID：**

在数据库页面顶部可以看到「数据库 ID」，复制备用。

### 第三步：创建 KV 命名空间

1. 点击左侧菜单 **「KV」**（在 Workers & Pages 下面）
2. 点击 **「创建命名空间」**
3. 名称填写：`fscx-kv`
4. 点击 **「添加」**

创建完成后，复制该命名空间的「ID」备用。

### 第四步：创建 Worker

1. 点击左侧 **「Workers & Pages」**
2. 点击 **「创建」** → **「Worker」**
3. 名称可填写：`fscx`（或任意你喜欢的名字）
4. 点击 **「部署」**

进入 Worker 编辑界面后：

1. 点击右上角 **「编辑代码」**
2. 将原来的默认代码**全部删除**
3. 打开 `fscx-d1.js` 文件，将**全部内容**复制粘贴进去
4. 点击右上角 **「部署」**

### 第五步：绑定 D1 数据库

1. 在 Worker 页面，点击 **「设置」** 标签
2. 找到 **「变量和机密」** → **「D1 数据库绑定」**
3. 点击 **「添加绑定」**
4. 变量名填写：`DB`（必须大写）
5. D1 数据库选择刚才创建的 `fscx-db`
6. 点击 **「保存」**

### 第六步：绑定 KV 命名空间

1. 在同一个「设置」页面，找到 **「KV 命名空间绑定」**
2. 点击 **「添加绑定」**
3. 变量名填写：`KV`（必须大写）
4. KV 命名空间选择刚才创建的 `fscx-kv`
5. 点击 **「保存」**

### 第七步：设置管理员密码（环境变量）

1. 在同一个「设置」页面，找到 **「变量和机密」** → **「机密」**
2. 点击 **「添加变量」**（或「添加机密」）
3. 变量名填写：`ADMIN_PASSWORD`（必须大写，完全一致）
4. 值填写你想要设置的管理员密码
5. 点击 **「加密」** 再点 **「保存」**

### 第八步：重新部署

回到 Worker 代码页面，点击 **「部署」** 使所有配置生效。

🎉 **完成！** 访问 `https://fscx.你的子域名.workers.dev` 即可使用。

---

## 本地开发与运行

> 如果你希望在本地电脑上运行、测试或二次开发，可以使用 Wrangler 的本地模拟模式。

### 环境要求

- Node.js 18+
- npm 或 pnpm

### 启动本地开发服务器

```bash
# 安装 wrangler（如已安装可跳过）
npm install -g wrangler

# 进入项目目录
cd my-fscx

# 初始化本地数据库
wrangler d1 execute fscx-db --local --file=schema.sql

# 启动本地开发服务器
wrangler dev
```

启动成功后，终端会显示本地地址（一般是 `http://localhost:8787`），在浏览器中打开即可。

### 本地设置管理员密码

本地开发时，在项目根目录创建 `.dev.vars` 文件（注意不要提交到 Git）：

```
ADMIN_PASSWORD=你的本地测试密码
```

### 关于本地模拟的说明

| 功能 | 本地支持情况 |
|------|------------|
| 成绩查询前台 | ✅ 完整支持 |
| 管理后台 | ✅ 完整支持 |
| 教师工作台 | ✅ 完整支持 |
| D1 数据库 | ✅ 本地 SQLite 模拟 |
| KV 存储 | ✅ 本地文件模拟 |
| 数据持久化 | ✅ 本地 `.wrangler/state/` 目录 |

> 💡 本地数据与线上数据是完全独立的，本地测试不会影响线上。

---

## 初始化数据库

无论哪种部署方式，首次使用前都需要执行 `schema.sql` 中的建表 SQL（见[部署方式一第七步](#第七步初始化数据库表结构)或[部署方式二第二步](#第二步创建-d1-数据库)）。

数据库初始化后，系统已内置以下默认配置：

- **默认查询字段**：姓名、准考证号、学校
- **默认考试方向**：历史方向（语数英政史地）、物理方向（语数英物化生）、职业高中（语数英信息技术）

这些配置可以在管理后台随时修改，无需重新初始化。

---

## 功能详解

### 🎓 学生前台（访问 `/`）

这是学生查成绩的入口，所有人都可访问，无需登录。

**查询流程：**

1. 在下拉框选择考试批次（由管理员创建并开放）
2. 填写必要信息（如准考证号、姓名、学校）
3. 点击「查询」按钮
4. 跳转到结果页显示成绩

**结果页功能：**

- 展示本次考试各科成绩及等次（≥120 优秀 / ≥90 良好 / ≥60 合格 / 不足 待提升）
- 显示个人信息卡（姓名、准考证号、考试方向、查询日期等）
- 若该准考证号参加过多次考试，自动展示**历次成绩对比**（以标签页形式切换）
- 提示可用浏览器打印（Ctrl+P）保存成绩单

**安全设计：**

- 查询参数通过 `sessionStorage` 传递，不暴露在 URL 中
- 结果页读取完参数后立即清除，防止刷新重复查询

---

### 👨‍🏫 教师工作台（访问 `/js`）

教师使用独立账号登录，可查看所负责班级的成绩数据。

**登录：**

- 输入教师编号和密码
- 支持"记住登录状态"（8小时内免登录）

**查询条件：**

- 选择考试批次
- 选择所负责的班级

**数据展示：**

- **统计卡片**：班级人数、年级总人数、年级班级数、班级科目均分
- **各科统计**：每科的班级平均分 + 在年级中的排名（如"年级第 3 / 8 班"）
- **学生成绩表**：按班级排名排序，显示每位同学各科成绩、总分、班排、年排

**权限区分：**

- **班主任**：可查看全部科目，可查看总分排名
- **科任老师**：仅能查看自己负责的那一科，看不到其他科目和总分

---

### 🛠 管理后台（访问 `/admin`）

管理员凭密码登录，拥有系统的全部管理权限。

**侧边栏导航（手机端自动折叠为顶部标签栏）：**

#### 📋 成绩查看

- 按考试批次筛选成绩记录
- 支持按姓名、准考证号等字段动态筛选
- 分页显示（每页 20/50/100/200 条或全部）
- 支持勾选多条记录后**批量删除**
- 点击某条记录可在弹出浮层中**在线编辑成绩**

#### ➕ 录入成绩

逐条手动录入单个学生的成绩：

1. 选择考试批次
2. 选择考试方向（系统根据方向自动显示对应科目输入框）
3. 填写学生信息字段（姓名、准考证号等）
4. 填写各科成绩
5. 提交

#### 📥 批量导入

支持从 Excel / CSV / 文本文件批量导入成绩，是最高效的录入方式。

**智能特性：**

- 支持 `.xlsx` / `.xls` / `.csv` / `.txt` 格式
- 多 Sheet 时弹窗让你选择要导入的 Sheet
- **自动识别列映射**：系统会自动把「姓名」「准考证号」「语文」「数学」等列头与系统字段匹配
- **自动跳过统计行**：平均分、最高分、汇总等行会被自动过滤
- **自动忽略排名列**：班次、校次、名次等列不会被导入
- **自动推断考试方向**：如果文件中没有"方向"列，系统根据每个学生的科目组合自动判断历史方向/物理方向等
- **预览确认**：导入前展示解析结果预览，标注警告和错误，确认无误后再导入
- **逐条写入**：导入过程显示进度条，失败条目单独提示，不影响成功条目

#### 🗂 考试批次

管理所有考试批次（如「2024年春季学业水平考试」）：

- 新增批次：设置批次 ID（英文唯一标识）、显示名称、考试日期
- 切换是否对前台开放查询（关闭后学生无法在前台选择该批次）
- 删除批次

#### 🔧 查询字段配置

自定义学生查询时需要填写的字段（如增加"班级"字段）：

- 添加、删除、重排字段
- 每个字段可设置：字段标签（如"姓名"）、占位提示、是否必填
- **isUid**：标记哪个字段是唯一标识（用于数据库索引，一般选"准考证号"）
- **isVerify**：标记哪些字段作为身份核验（填写内容必须与数据库记录一致才能查到成绩，防止别人随意查别人的成绩）

#### 📚 方向科目配置

管理考试方向及各方向包含的科目：

- 添加/删除考试方向（如"物理方向"）
- 为每个方向配置科目列表（如"语文、数学、英语、物理、化学、生物"）
- 科目顺序可拖拽调整

#### 🎨 页面设置

自定义前台页面的所有文字内容，无需改代码：

- 浏览器标签页标题
- 页面顶部大标题和副标题
- 首页通知公告文字
- 底部版权文字
- 导航栏标签名称（"成绩查询""查询说明"等）
- 查询说明页的说明文字内容

#### 📊 统计概览

- 系统中学生总数（去重计算）
- 成绩记录总数
- 考试批次总数
- 各批次录入情况明细（点击可展开查看或快速编辑）

#### 👨‍🏫 教师账号管理

创建和管理教师工作台的登录账号：

- 新增教师：设置编号、姓名、密码、账号类型、负责科目、管理班级
- 账号类型：
  - **班主任**：可查看全部科目，不限制班级
  - **科任老师**：只能查看指定科目，只能查看分配给他的班级
- 编辑教师信息或重置密码
- 删除教师账号
- 页面底部显示教师登录入口链接

---

## 安全机制说明

| 机制 | 说明 |
|------|------|
| **暴力破解防护** | 同一 IP 连续登录失败 10 次后，锁定 15 分钟 |
| **Token 认证** | 登录后签发随机 Token，后续请求凭 Token 鉴权，密码不在每次请求中传输 |
| **Token 有效期** | 管理员普通登录 8 小时，勾选「记住我」7 天；教师登录 8 小时 |
| **XSS 防护** | 所有来自数据库或用户输入的内容在拼入 HTML 前均经过转义处理 |
| **成绩核验** | 查询时需提供核验字段（如姓名），与数据库不匹配则拒绝返回成绩 |
| **公开接口限流** | 学生查询接口同样有频率限制，防止批量枚举准考证号 |
| **密码加密存储** | 教师密码以 SHA-256 哈希存储，不存储明文 |
| **管理员密码** | 通过 Cloudflare Secret 存储，不进入代码或版本控制 |

---

## 常见问题

**Q：我访问页面显示"服务不可用"或报错怎么办？**

> A：请检查 D1 数据库和 KV 命名空间是否正确绑定，绑定变量名必须严格是 `DB` 和 `KV`（区分大小写）。同时确认数据库已执行过 `schema.sql` 初始化脚本。

**Q：忘记管理员密码怎么办？**

> A：在 Cloudflare 后台 → Worker 设置 → 环境变量，找到 `ADMIN_PASSWORD` 机密，重新设置一个新密码即可。

**Q：学生查询提示"信息不匹配"？**

> A：这是正常的安全机制。学生填写的"核验字段"（默认是姓名和学校）必须与数据库中录入的内容完全一致（包括空格和全半角），才能查到成绩。请核查录入的数据是否正确。

**Q：批量导入时有些学生失败了怎么办？**

> A：导入结果页会列出所有失败条目及原因。常见原因是准考证号格式不合法（含有冒号、斜杠等特殊字符）或成绩值无法解析。修正后重新导入该学生即可，已成功的记录不会被覆盖（除非准考证号相同）。

**Q：能不能部署在自己的服务器上而不用 Cloudflare？**

> A：本项目目前强依赖 Cloudflare Workers 运行环境（D1、KV、`env` 变量注入等 Cloudflare 专有 API），无法直接运行在普通 Node.js 服务器上。如需私有化部署，需将数据层替换为普通 SQLite 或其他数据库，并用 Express/Hono 等框架适配，改动较大。

**Q：免费版 Cloudflare Workers 够用吗？**

> A：Cloudflare Workers 免费版每天有 10 万次请求额度，D1 免费版有 500MB 存储和每天 2500 万次读取。对于一所学校的成绩查询场景（集中在几天内，学生数量有限），完全足够，无需付费。

**Q：如何自定义域名？**

> A：在 Cloudflare 的 Worker 设置页面 → 「触发器」→「添加自定义域」，前提是你的域名已经托管在 Cloudflare 上。

---

## 📄 文件说明

| 文件 | 说明 |
|------|------|
| `fscx-d1.js` | 主程序（全部后端逻辑 + 前端 HTML/CSS/JS 均在此文件内） |
| `schema.sql` | 数据库初始化 SQL，首次部署时执行一次 |
| `wrangler.toml` | Wrangler CLI 部署配置，包含 Worker 名称、D1 和 KV 绑定 |

---

## 🔗 快速入口

部署完成后的各功能入口：

| 地址 | 说明 |
|------|------|
| `https://你的域名/` | 学生成绩查询首页 |
| `https://你的域名/result` | 查询结果页（由查询页跳转） |
| `https://你的域名/help` | 查询说明页 |
| `https://你的域名/js` | 教师工作台登录页 |
| `https://你的域名/admin` | 管理员后台登录页 |

---

*如有问题或建议，欢迎提 Issue 或 PR。*
](https://blog.112323zxcv.dpdns.org/article/000006/.html)
